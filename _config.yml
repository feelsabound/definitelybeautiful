domain: feelsabound.com     # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: http://feelsabound.com #the base hostname and protocol for your site, e.g. http://example.com
# baseurl: /definitelybeautiful      # place folder name if the site is served in a subfolder